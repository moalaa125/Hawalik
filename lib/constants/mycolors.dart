import 'package:flutter/material.dart';

class Mycolors {
    static const Color blue = Color(0xFF0666EB);
    static const Color LighBlue = Color(0xFFE5EFFD);
    static const Color LightGrey = Color(0xFF0E1E1);
}